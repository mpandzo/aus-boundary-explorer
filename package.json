{
  "name": "maplibre-lga-explorer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Fastify server for the Australian LGA MapLibre + PMTiles demo",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "scripts/build-all.sh",
    "start": "node server.js"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "fastify": "^5.0.0"
  }
}
